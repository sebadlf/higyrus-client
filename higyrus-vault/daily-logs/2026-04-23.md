---
date: 2026-04-23
tags: [daily]
---

# Daily Log — 2026-04-23

## Foco del día
- [x] Bootstrap del repo `higyrus-client` (setup equivalente al de matriz-client: vault, GitHub, Linear, Claude Code)
- [x] Bootstrap del paquete Python (uv + ruff + pyright + pytest, `POST /api/login` Bearer auth, `get_health()`)
- [x] Crear backlog inicial en Linear project `Higyrus Client` (BEC-77 a BEC-81)
- [x] BEC-80 — Foundation: modelos safe-access
- [x] BEC-81 — Foundation: helpers de params (fechas, bools, drop_none)
- [x] BEC-78 — `GET /api/cuentas/{idCuenta}/posiciones` (primer endpoint end-to-end)

## Lo que hice

- Armé la estructura completa del vault `higyrus-vault/` con los mismos módulos que `matriz-vault` (HOME, workflows, knowledge, projects, daily-logs, retrospectives, templates).
- Setup de GitHub: `pull_request_template.md` + workflows `ci.yml`, `auto-merge.yml`, `release.yml`. Todos con el flujo Python (uv).
- Bootstrap del paquete `higyrus_client/`: cliente stateful con auth Bearer, 14 tests, pasa ruff + pyright limpio.
- Cargué en Linear (team `Becerra`, project `Higyrus Client`):
  - BEC-77, BEC-78, BEC-79 — endpoints individuales de `/api/cuentas/{id}/...`
  - BEC-80, BEC-81 — foundation (modelos + helpers de params), bloqueando los 3 anteriores
- Mergeé PR #1 (`chore: bootstrap Python package + Linear project references`).
- **BEC-80** implementado en la branch `sebadlf-bec-80-foundation-modelos-safe-access-frozen-dataclasses-para`:
  - `higyrus_client/models.py` con `SafeModel` + `_coerce` + modelos piloto `Posicion` y `Parking`
  - `pyproject.toml`: `per-file-ignores` para permitir camelCase en `models.py`
  - 26 tests nuevos en `tests/test_models.py` cubriendo scalar/list/nested/Optional coercion + casos del payload real de posiciones
  - ADR-001 escrito (link abajo)
- **BEC-81** implementado en la branch `sebadlf-bec-81-foundation-helpers-de-parametros-de-query-fechas-bools`:
  - `higyrus_client/_params.py` con `format_date` (`dd/mm/yyyy`), `format_bool` (`"True"`/`"False"`) y `drop_none`
  - `client.py` ahora consume `drop_none` desde `_params` — se eliminó la versión local
  - 15 tests nuevos en `tests/test_params.py` — total del repo 55
  - Domain note `Convenciones de query params.md` explicando formatos de fecha/bool, policy de "no validar client-side" y por qué no hacemos snake→camel automático
- **BEC-78** implementado en la branch `sebadlf-bec-78-implementar-get-apicuentasidcuentaposiciones`:
  - Wrapper público `get_posiciones(id_cuenta, fecha, *, especie=None, incluir_parking=False)` en `client.py`, exportado desde el paquete
  - Consume directamente `Posicion` + `Parking` (piloto de BEC-80) y los helpers de `_params` (BEC-81) — foundation funcionó como esperaba
  - 10 tests nuevos en `tests/test_cuentas.py` (nueva convención: tests por dominio). Total del repo 65
  - `main.py` con ejemplo comentado para probar contra sandbox

## Aprendizajes

- **Linear↔GitHub integration tenía scope al repo viejo**: al crear issues en el project `Higyrus Client` (team `BEC`), Linear abrió GitHub issues fantasma en `sebadlf/matriz-client` (#97–#101). Fix: reconfigurar la integración con `sebadlf/higyrus-client` en Team settings. Vale documentar esto como runbook la próxima vez que un team de Linear sirva a varios repos.
- **`enablePullRequestAutoMerge` hay que habilitarlo en settings del repo** (`Settings → General → Pull Requests → Allow auto-merge`). El workflow `auto-merge.yml` falla con `GraphQL: Auto merge is not allowed for this repository` hasta que se habilita. No es parte de Branch protection — es un toggle aparte.
- **Pydantic no era la mejor opción** — validación estricta pelea con el modelo de keys condicionales de Higyrus. Frozen dataclasses + `from_api` resultaron más simples, sin dependencias, y alcanzan el objetivo (chaining seguro). Ver ADR-001.
- **La foundation valió lo que costó**: en BEC-78 el wrapper `get_posiciones` terminó siendo ~15 líneas efectivas. Todo el trabajo de parsing, defaults, serialización de fecha/bool y drop de `None` ya estaba hecho en BEC-80 y BEC-81. Los próximos endpoints de lectura deberían ser igual de chicos.
- **Convención adoptada**: tests por dominio (`test_cuentas.py`) en vez de por archivo Python (`test_client.py`). `test_client.py` queda para tests del transporte (`_request`, `_raise_for_response`, login); los endpoints públicos agrupados por sistema Higyrus (`cuentas`, `personas`, `operaciones`, etc.).

## Blockers / Preguntas

- **Formato de `fecha` en `/api/cuentas/{idCuenta}/posiciones`** sigue sin especificar en el PDF — asumimos `dd/mm/yyyy` pero hay que confirmar contra sandbox cuando se implemente BEC-78.
- **Status 201 en un GET** (`/api/cuentas/{idCuenta}/posicionValuada`) — probable inconsistencia del doc. Validar al implementar BEC-79.

## Notas para mañana

- Primer endpoint de lectura (`get_posiciones`) completo end-to-end. Quedan **BEC-77** (`/movimientos`) y **BEC-79** (`/posicionValuada`) de los tickets originales — ambos siguen el mismo patrón.
- **Pendiente: validar contra sandbox el formato de `fecha` para `/posiciones`**. El PDF no lo especifica; asumimos `dd/mm/yyyy`. Si se confirma algo distinto, actualizar `_params.format_date` (o agregar variante) y `Convenciones de query params.md`.
- Revisar si el repo `sebadlf/higyrus-client` necesita `Allow auto-merge` en settings antes del próximo PR, así los próximos mergean solos.

## Documentación generada

- `higyrus-vault/projects/higyrus-client/decisions/ADR-001 — Modelos safe-access.md`
- `higyrus-vault/projects/higyrus-client/domain/Convenciones de query params.md`
- Entradas en `higyrus-vault/projects/higyrus-client/README.md` (Decisiones y Notas de dominio).
- `tests/test_cuentas.py` establece la convención de tests por dominio Higyrus.
